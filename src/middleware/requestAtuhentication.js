const requestAuthentication = async (req, res) => {
	try {
		const header = req.header;
		return
	} catch (error) {
		console.log('something went wrong in request authentication');
		throw error;
	}
}

module.exports = requestAuthentication;